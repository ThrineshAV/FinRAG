# FinSight-RAG environment configuration (template)
# Copy to .env and fill in real values. Never commit .env.

# --- LLM ---
# Gemini (Google) — required for grounded answer generation
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# --- Auth ---
# JWT (HS256) — required for /auth endpoints
JWT_SECRET=change-me-to-a-long-random-string
JWT_COOKIE_SECURE=false

# Admin bootstrap on first run (used by auth/database.py:seed_admin_from_env)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# --- Cache ---
CACHE_ENABLED=true
REDIS_URL=redis://localhost:6379/0
CACHE_TTL_SECONDS=3600

# --- Rate limits ---
RATE_LIMIT_QUERY=20/minute
RATE_LIMIT_UPLOAD=5/minute
MAX_UPLOAD_SIZE_MB=25
